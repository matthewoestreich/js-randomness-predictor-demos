{
  "name": "js-randomness-predictor-demos",
  "version": "1.0.0",
  "description": "Demos and examples for js-randomness-predictor",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "prettier": {
    "semi": true,
    "singleQuote": false,
    "printWidth": 150
  },
  "scripts": {
    "format": "prettier . --write",
    "build:root": "node build.js",
    "build:react:vite": "cd react/vite && npm run build:prod && cd ../..",
    "build:vanillajs": "cd vanillajs && node build.js && cd ../",
    "build": "rimraf docs && npm run build:root && npm run build:react:vite && npm run build:vanillajs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1"
  }
}
