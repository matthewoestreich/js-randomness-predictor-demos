{
  "name": "js-randomness-predictor-demos",
  "version": "1.0.0",
  "description": "Demos and examples for js-randomness-predictor",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "prettier": {
    "semi": true,
    "singleQuote": false,
    "printWidth": 150
  },
  "scripts": {
    "format": "prettier . --write",
    "build:root": "node build.js",
    "build:vue:vite": "npm --prefix vue/vite run build",
    "build:solidjs:vite": "npm --prefix solidjs/vite run build",
    "build:react:vite": "cd react/vite && npm run build && cd ../..",
    "build:react:webpack": "npm --prefix react/webpack run build",
    "build:vanillajs": "cd vanillajs && node build.js && cd ../",
    "build": "rimraf docs && npm run build:root && npm run build:react:vite && npm run build:react:webpack && npm run build:vue:vite && npm run build:vanillajs",
    "build:dev": "rimraf docs && npm run build:root && DEV=1 npm run build:react:vite && DEV=1 npm run build:vue:vite && DEV=1 npm run build:solidjs:vite && npm run build:react:webpack && npm run build:vanillajs",
    "gh:push": "npm run format && npm run build && git add . && git commit -m \"update\" && git push origin main",
    "dev": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
